---
phase: 04-seo-infrastructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/sitemap.ts
  - src/app/robots.ts
  - src/app/layout.tsx
  - .env.example
  - docs/google-search-console-setup.md
autonomous: true
requirements:
  - SEO-08
  - INFRA-02

must_haves:
  truths:
    - "Visiting /sitemap.xml returns valid XML with all 8 core page URLs and image references"
    - "Visiting /robots.txt returns valid directives allowing crawling of public pages and blocking /studio/ and /api/"
    - "When NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION env var is set, a google-site-verification meta tag renders in the HTML head"
    - "Emily has a step-by-step guide for setting up Google Search Console, adding the verification code to Vercel, and submitting the sitemap"
    - "The existing /api/revalidate webhook endpoint validates Sanity signatures and triggers ISR cache purge correctly"
  artifacts:
    - path: "src/app/sitemap.ts"
      provides: "XML sitemap generation for all pages with image references"
      contains: "MetadataRoute.Sitemap"
    - path: "src/app/robots.ts"
      provides: "robots.txt generation with sitemap reference"
      contains: "MetadataRoute.Robots"
    - path: "docs/google-search-console-setup.md"
      provides: "Non-technical GSC setup guide for Emily"
      contains: "google-site-verification"
  key_links:
    - from: "src/app/sitemap.ts"
      to: "src/lib/siteConfig.ts"
      via: "import siteConfig.url for base URL"
      pattern: "siteConfig\\.url|emilykathryn\\.com"
    - from: "src/app/robots.ts"
      to: "src/app/sitemap.ts"
      via: "sitemap URL reference"
      pattern: "sitemap.*emilykathryn\\.com/sitemap\\.xml"
    - from: "src/app/layout.tsx"
      to: "NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION env var"
      via: "metadata.verification.google"
      pattern: "verification.*google"
---

<objective>
Create XML sitemap, robots.txt, Google Search Console verification support, and validate the existing ISR webhook — completing the discovery and indexing infrastructure needed before city landing pages are built.

Purpose: Search engines need a sitemap to discover all pages, robots.txt to know crawl boundaries, and GSC verification to receive indexing reports. The ISR webhook must be confirmed working so Emily's CMS edits are reflected live. Together these maximize the site's visibility in Google and prepare the foundation for Phase 5 city pages.

Output: sitemap.ts, robots.ts, GSC verification in root layout metadata, updated .env.example, GSC setup guide for Emily, validated ISR webhook.
</objective>

<execution_context>
@/Users/jeff/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeff/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-seo-infrastructure/04-CONTEXT.md
@.planning/phases/04-seo-infrastructure/04-RESEARCH.md
@src/app/layout.tsx
@src/app/api/revalidate/route.ts
@src/lib/siteConfig.ts
@.env.example
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create sitemap, robots.txt, and GSC verification</name>
  <files>
    src/app/sitemap.ts
    src/app/robots.ts
    src/app/layout.tsx
    .env.example
  </files>
  <action>
Create three SEO infrastructure files using Next.js built-in conventions, plus update root layout for GSC verification.

**1. `src/app/sitemap.ts`** — XML sitemap using Next.js built-in convention:
- Export default function returning `MetadataRoute.Sitemap`
- Use `siteConfig.url` (`https://emilykathryn.com`) as BASE_URL — import from `@/lib/siteConfig`
- Include all 8 core pages with appropriate priority and changeFrequency:
  - `/` — priority 1, changeFrequency 'weekly', images: [`${BASE_URL}/og/default.jpg`]
  - `/senior-portraits` — priority 0.9, changeFrequency 'monthly', images: [`${BASE_URL}/og/senior-portraits.jpg`]
  - `/family-portraits` — priority 0.9, changeFrequency 'monthly', images: [`${BASE_URL}/og/family-portraits.jpg`]
  - `/contact` — priority 0.8, changeFrequency 'monthly', images: [`${BASE_URL}/og/contact.jpg`]
  - `/investment` — priority 0.8, changeFrequency 'monthly', images: [`${BASE_URL}/og/investment.jpg`]
  - `/about` — priority 0.7, changeFrequency 'monthly', images: [`${BASE_URL}/og/about.jpg`]
  - `/raves` — priority 0.6, changeFrequency 'weekly', images: [`${BASE_URL}/og/raves.jpg`]
  - `/style-guide` — priority 0.5, changeFrequency 'monthly', images: [`${BASE_URL}/og/style-guide.jpg`]
- Include `lastModified: new Date()` on all entries
- Add a comment noting that city pages (Phase 5) will be added to this sitemap when built
- NOTE: Include image sitemaps per research recommendation — for a photography business, image search is a significant traffic driver. The `images` array in each sitemap entry generates `<image:image>` XML tags.
- CRITICAL: Place in `src/app/` root, NOT inside `(site)/` route group — route groups don't affect URL but sitemap.ts must be at app root to generate at `/sitemap.xml`

**2. `src/app/robots.ts`** — robots.txt using Next.js built-in convention:
- Export default function returning `MetadataRoute.Robots`
- Rules: `userAgent: '*'`, `allow: '/'`, `disallow: ['/studio/', '/api/']`
- Sitemap: `'https://emilykathryn.com/sitemap.xml'` (use siteConfig.url)
- CRITICAL: Place in `src/app/` root (same reason as sitemap)

**3. `src/app/layout.tsx`** — Add GSC verification to root layout metadata:
- The root layout already has `export const metadata: Metadata = { ... }`
- Add `verification` field to the existing metadata object:
  ```
  verification: {
    google: process.env.NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION,
  },
  ```
- This renders `<meta name="google-site-verification" content="...">` when the env var is set
- When env var is empty/undefined, Next.js omits the meta tag entirely (no empty content="" issue)
- Do NOT modify any other part of the root layout

**4. `.env.example`** — Add new environment variables:
- Add `NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION=` with a comment explaining it's from Google Search Console
- Add `RESEND_API_KEY=` if not already present (used by Phase 3 inquiry form)
- Keep existing env vars unchanged
  </action>
  <verify>
    <automated>cd "/Users/jeff/AI Projects/NXTLVL-Digital/Clients/EmilyKathryn" && npx tsc --noEmit src/app/sitemap.ts src/app/robots.ts src/app/layout.tsx 2>&1 | head -20</automated>
    <manual>Verify sitemap.ts and robots.ts are in src/app/ root (not inside route groups); verify root layout metadata has verification.google field</manual>
  </verify>
  <done>sitemap.ts generates valid sitemap with all 8 pages and image references; robots.ts allows public crawling and blocks /studio/ and /api/; root layout metadata includes google-site-verification when env var is set; .env.example documents all required env vars</done>
</task>

<task type="auto">
  <name>Task 2: Validate ISR webhook and create GSC setup guide</name>
  <files>
    docs/google-search-console-setup.md
  </files>
  <action>
**1. Validate ISR webhook** — The existing `/api/revalidate` endpoint at `src/app/api/revalidate/route.ts` already:
- Validates Sanity webhook signatures via `parseBody` from `next-sanity/webhook`
- Checks `SANITY_REVALIDATE_SECRET` env var
- Calls `revalidateTag()` for each tag in the payload
- Returns JSON `{ revalidated: true, tags: [...] }` on success

Validation is code review only — the webhook is functional. Confirm by reading the file and verifying:
- parseBody is imported from 'next-sanity/webhook' (correct import path)
- Signature validation happens before any tag processing
- Error responses use appropriate HTTP status codes (401 for invalid signature, 400 for bad payload, 500 for server error)
- The webhook filter comment documents the correct document types and projection

No code changes needed for the webhook — it was correctly implemented in Phase 1.

**2. Create GSC setup guide** — Write `docs/google-search-console-setup.md`:
- Title: "Google Search Console Setup Guide"
- Written for Emily (non-technical user) with numbered steps
- Section 1: "Create Your Google Search Console Account"
  - Go to https://search.google.com/search-console/about
  - Sign in with your Google account (the one you use for Google Business Profile if possible)
  - Click "Add a property"
  - Choose "URL prefix" method
  - Enter `https://emilykathryn.com`
- Section 2: "Get Your Verification Code"
  - On the verification page, choose "HTML tag" verification method
  - Copy the `content` value from the meta tag (just the code, not the full HTML tag)
  - It looks like a string of letters and numbers (e.g., `abc123def456`)
- Section 3: "Add the Code to Your Website"
  - Go to https://vercel.com and log in
  - Navigate to your Emily Kathryn Photography project
  - Go to Settings > Environment Variables
  - Add a new variable:
    - Name: `NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION`
    - Value: paste the verification code from Step 2
    - Environment: Production (check), Preview (check), Development (optional)
  - Click Save
  - Trigger a new deployment (go to Deployments tab, click "Redeploy" on the latest)
- Section 4: "Verify Ownership"
  - Go back to Google Search Console
  - Click "Verify"
  - Should show "Ownership verified" (may take a few minutes after redeployment)
- Section 5: "Submit Your Sitemap"
  - In Google Search Console, click "Sitemaps" in the left sidebar
  - Enter `sitemap.xml` in the "Add a new sitemap" field
  - Click "Submit"
  - Status should change to "Success" (may take a few minutes to process)
- Section 6: "What Happens Next"
  - Google will start crawling and indexing your pages over the next few days to weeks
  - You can check the "Coverage" report to see which pages are indexed
  - The "Performance" report shows search queries and clicks once data accumulates (usually 2-4 weeks)
- Keep the tone friendly, clear, and encouraging — matching Emily's relationship with her developer
  </action>
  <verify>
    <automated>cd "/Users/jeff/AI Projects/NXTLVL-Digital/Clients/EmilyKathryn" && test -f docs/google-search-console-setup.md && echo "GSC guide exists" && test -f src/app/api/revalidate/route.ts && echo "Webhook exists" && grep -c "parseBody" src/app/api/revalidate/route.ts</automated>
    <manual>Read the GSC setup guide and verify it is clear enough for a non-technical person to follow independently; confirm webhook code review found no issues</manual>
  </verify>
  <done>ISR webhook validated as correctly implemented (no changes needed); GSC setup guide exists at docs/google-search-console-setup.md with step-by-step instructions for Emily covering account creation, verification code, Vercel env var, verification, and sitemap submission</done>
</task>

</tasks>

<verification>
1. `npx next build` completes without errors
2. `npx tsc --noEmit` passes for sitemap.ts, robots.ts, and layout.tsx
3. sitemap.ts is in `src/app/` root (not inside a route group)
4. robots.ts is in `src/app/` root (not inside a route group)
5. Root layout metadata includes `verification.google` field reading from env var
6. .env.example includes NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION
7. GSC setup guide is complete and non-technical
8. ISR webhook at /api/revalidate is confirmed functional (code review)
</verification>

<success_criteria>
- /sitemap.xml generates valid XML with all 8 core page URLs and image:image tags for OG images
- /robots.txt allows public crawling, blocks /studio/ and /api/, references sitemap
- GSC verification meta tag renders when NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION is set
- Emily has actionable GSC setup guide she can follow independently
- ISR webhook confirmed functional with proper signature validation
- Build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-seo-infrastructure/04-02-SUMMARY.md`
</output>
