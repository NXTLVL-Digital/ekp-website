---
phase: 05-city-landing-pages
plan: 03
type: execute
wave: 3
depends_on:
  - 05-02
files_modified: []
autonomous: false
requirements:
  - LOCAL-01
  - LOCAL-02
  - LOCAL-03
  - LOCAL-04
  - LOCAL-05
  - LOCAL-06
  - LOCAL-07
  - SEO-05
  - SEO-06

must_haves:
  truths:
    - "All 7 city pages render correctly with hero, AEO block, body copy, map facade, CTA, and JSON-LD"
    - "Visual design matches the editorial brand aesthetic (warm, confident, gold accents)"
    - "No two city pages share more than 40% identical body content"
    - "Mobile responsive layout works on all city pages"
    - "Google Maps facade loads the real map on click interaction"
    - "JSON-LD structured data is valid for each city page"
  artifacts: []
  key_links: []
---

<objective>
Verify all 7 city landing pages visually, audit content uniqueness across all 21 city-pair combinations, and confirm JSON-LD validity and Lighthouse performance.

Purpose: Human-in-the-loop verification ensures city pages meet visual quality standards, content uniqueness requirements (no two pages > 40% similar), and SEO infrastructure is correctly wired before shipping.
Output: Verified and approved city landing pages ready for Phase 6.
</objective>

<execution_context>
@/Users/jeff/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeff/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-city-landing-pages/05-CONTEXT.md
@.planning/phases/05-city-landing-pages/05-01-SUMMARY.md
@.planning/phases/05-city-landing-pages/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated verification of city pages (build, JSON-LD, sitemap, uniqueness)</name>
  <files></files>
  <action>
    Run a comprehensive automated verification suite:

    **1. Production build** — `npx next build` must pass with all 7 city pages in the output.

    **2. JSON-LD validation** — For each city page, verify the built HTML output contains a JSON-LD script tag with:
    - `@type: ProfessionalService`
    - `@id` containing the city slug (e.g., `/chatham/#business`)
    - `areaServed` with the correct city name
    - `geo.latitude` and `geo.longitude` matching CITY_DATA values
    - Use grep on the build output files in `.next/server/app/(site)/[city]/` to verify

    **3. Sitemap verification** — Check that `src/app/sitemap.ts` is async and produces entries for all 7 city slugs. Verify by inspecting the sitemap function logic or running a simple import test.

    **4. Content uniqueness audit** — Write a quick inline script to:
    - Extract the body text from each city's CITY_CONTENT entry
    - Strip HTML tags to get plain text
    - For all 21 pair combinations (7 choose 2), compute a simple word-overlap similarity:
      - Tokenize both texts into words (lowercased, stripped of punctuation)
      - Calculate Jaccard similarity: |intersection| / |union|
      - Report pairs exceeding 40% similarity
    - Output a table of all 21 pairs with their similarity scores
    - ALL pairs must be below 40% to pass

    **5. Word count verification** — For each city, count words in the body copy and verify:
    - Danville, Lynchburg: 450-550 words
    - Chatham, Smith Mountain Lake: 300-450 words
    - Forest, Altavista, Evington: 200-350 words

    **6. AEO block verification** — For each city, verify:
    - Contains "Emily Kathryn Photography" (exact business name)
    - Word count is between 35-65 words
    - Contains city name

    Report all results as a verification summary table.
  </action>
  <verify>
    <automated>cd "/Users/jeff/AI Projects/NXTLVL-Digital/Clients/EmilyKathryn" && npx next build 2>&1 | tail -10</automated>
    <manual>Review the uniqueness and word count report tables</manual>
  </verify>
  <done>Production build passes, all 7 city pages present, JSON-LD contains correct geo and areaServed, sitemap includes all city URLs, no pair exceeds 40% content similarity, word counts are within tier targets, AEO blocks are valid</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human visual and content verification of city landing pages</name>
  <what-built>
    All 7 city landing pages with:
    - Text-focused hero with city-specific headline
    - AEO answer block (always-visible, gold-accented callout)
    - Unique SEO body copy per city in Emily's brand voice
    - Gallery grid placeholder (will show when CMS images are assigned)
    - Testimonials placeholder (will show when CMS testimonials are assigned)
    - Google Maps facade (click-to-load)
    - Gold CTA button linking to contact page
    - City-specific LocalBusiness JSON-LD with geo coordinates
    - All 7 city URLs in the sitemap
  </what-built>
  <how-to-verify>
    Start the dev server: `npm run dev`

    **Visual check (spot-check 3 cities, one per tier):**
    1. Visit http://localhost:3000/lynchburg (large market)
       - Hero has "Senior Portraits in Lynchburg, VA" headline with branded gradient background
       - AEO block is visible as a callout box (gold-tinted, left border accent)
       - Body copy reads naturally, mentions both Senior and Family portraits
       - Google Maps placeholder shows "Click to view map" button
       - Gold CTA button visible at bottom
    2. Visit http://localhost:3000/chatham (mid market)
       - Different body copy from Lynchburg (genuinely unique, not template-swapped)
       - Shorter content than Lynchburg (mid-tier word count)
       - Same layout structure but different content character
    3. Visit http://localhost:3000/forest (small market)
       - Even shorter content than Chatham
       - Still reads naturally, not truncated

    **404 check:**
    4. Visit http://localhost:3000/richmond — Should return 404 page

    **Mobile check:**
    5. Open http://localhost:3000/danville on mobile viewport (Chrome DevTools)
       - All sections stack properly
       - Text is readable, no horizontal overflow
       - CTA button has adequate tap target

    **Content quality check:**
    6. Read the AEO blocks on at least 3 city pages
       - Each should feel like a unique, authoritative answer snippet
       - "Emily Kathryn Photography" business name used (not "Emily" casually)

    **Map interaction (optional, requires API key):**
    7. If NEXT_PUBLIC_GOOGLE_MAPS_EMBED_KEY is configured, click the map facade on any city page to verify the Google Maps iframe loads correctly centered on that city

    **JSON-LD check:**
    8. View page source on http://localhost:3000/danville
       - Find the `<script type="application/ld+json">` tag
       - Should contain `"@type": "ProfessionalService"`, `"areaServed"` with Danville only, `"geo"` with coordinates
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 5, or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. Production build passes with all 7 city pages
2. No content pair exceeds 40% Jaccard similarity
3. Word counts match tier targets
4. JSON-LD contains unique areaServed and geo per city
5. Human confirms visual quality, mobile responsiveness, and content voice
6. Sitemap includes all 7 city URLs
</verification>

<success_criteria>
- Automated tests all pass (build, uniqueness, word counts, JSON-LD, AEO blocks)
- Human approves visual design, content quality, and mobile responsiveness
- All phase requirements verified (LOCAL-01 through LOCAL-07, SEO-05, SEO-06)
</success_criteria>

<output>
After completion, create `.planning/phases/05-city-landing-pages/05-03-SUMMARY.md`
</output>
