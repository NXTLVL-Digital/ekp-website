---
phase: 02-shared-components-and-galleries
plan: 03
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/app/(site)/gallery-test/page.tsx
  - src/components/shared/GalleryGrid.tsx
  - src/components/shared/GalleryClient.tsx
  - src/components/shared/GalleryLightbox.tsx
autonomous: false
requirements:
  - PERF-01
  - PERF-02
  - PERF-03
  - GALL-04

must_haves:
  truths:
    - "Gallery test page with 20+ images achieves Lighthouse mobile Performance score of 100 (development-time proxy for PERF-03 Vercel Speed Insights target)"
    - "Gallery test page has LCP under 2.5 seconds on Lighthouse mobile audit"
    - "Gallery test page has CLS under 0.1 on Lighthouse mobile audit"
    - "All interactive gallery elements (lightbox open/close, nav arrows) have minimum 44x44px tap targets"
    - "Lightbox opens on image click, navigates with arrow keys and swipe, shows counter, closes with Escape"
    - "@vercel/speed-insights is installed and SpeedInsights component is rendered in the site layout, ready to collect real-user data once deployed"
  artifacts:
    - path: "src/app/(site)/gallery-test/page.tsx"
      provides: "Gallery test page verified against Lighthouse mobile performance targets"
      contains: "gallery-test"
  key_links:
    - from: "Lighthouse audit"
      to: "src/app/(site)/gallery-test/page.tsx"
      via: "URL: http://localhost:3000/gallery-test"
      pattern: "gallery-test"
    - from: "src/app/(site)/layout.tsx"
      to: "@vercel/speed-insights"
      via: "SpeedInsights component rendered in layout"
      pattern: "SpeedInsights"
---

<objective>
Run Core Web Vitals verification on the gallery test page: Lighthouse mobile audit targeting 100/100 performance score, LCP, CLS, tap target and lightbox interaction verification. Verify @vercel/speed-insights is wired up for production real-user monitoring. Fix any issues found.

Purpose: Phase 2 success criteria require LCP under 2.5s, CLS under 0.1, and 100/100 mobile performance. Lighthouse 100 is used as the development-time proxy target. PERF-03 specifies Vercel Speed Insights 100/100 which requires real deployed traffic -- this plan ensures the best possible Lighthouse score locally and confirms Speed Insights infrastructure is in place for post-deployment verification.

Output: Verified gallery test page meeting Core Web Vitals targets, any performance fixes applied, Speed Insights readiness confirmed
</objective>

<execution_context>
@/Users/jeff/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeff/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-shared-components-and-galleries/02-RESEARCH.md
@.planning/phases/02-shared-components-and-galleries/02-01-SUMMARY.md

Key performance targets:
- PERF-01: LCP under 2.5 seconds on mobile
- PERF-02: CLS under 0.1
- PERF-03: 100/100 mobile performance (Vercel Speed Insights -- requires deployed traffic; Lighthouse 100 used as development proxy)
- GALL-04: All lightbox interactions working with 44x44px tap targets

Speed Insights status from Phase 1:
- @vercel/speed-insights is already installed (package.json)
- SpeedInsights component is already rendered in src/app/(site)/layout.tsx
- Final PERF-03 verification requires a Vercel preview deployment with real traffic -- this cannot be done locally
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lighthouse mobile audit, performance fixes, and Speed Insights verification</name>
  <files>
    src/app/(site)/gallery-test/page.tsx
    src/components/shared/GalleryGrid.tsx
    src/components/shared/GalleryClient.tsx
    src/components/shared/GalleryLightbox.tsx
  </files>
  <action>
Run Lighthouse audit on the gallery test page, fix any issues, and verify Speed Insights infrastructure.

**Step 1: Verify @vercel/speed-insights is installed and wired up**
- Confirm `@vercel/speed-insights` is in package.json dependencies
- Confirm `src/app/(site)/layout.tsx` imports and renders `<SpeedInsights />` from `@vercel/speed-insights/next`
- If either is missing, install the package and add the component to the layout
- Note: Speed Insights only collects real-user metrics on deployed Vercel instances. It will not produce data locally. This step confirms the infrastructure is ready for production.

**Step 2: Build and start production server**
```bash
npm run build && npm run start
```

**Step 3: Run Lighthouse CLI mobile audit**
```bash
npx lighthouse http://localhost:3000/gallery-test --only-categories=performance --output=json --output-path=./lighthouse-gallery-mobile.json --chrome-flags="--headless"
```

**Step 4: Analyze results -- target is 100, not 90**
Check the JSON output for:
- Performance score (target: 100)
- LCP (target: under 2.5s)
- CLS (target: under 0.1)
- TBT (Total Blocking Time -- should be near 0 with Server Components)
- Any flagged issues: unused CSS, render-blocking resources, improperly sized images

**Step 5: Fix any issues to reach 100**
Common fixes based on research anti-patterns:
- If LCP is high: Ensure no gallery images have `priority` prop (only hero images should). Check that the first visible content is not a large unoptimized image. Consider adding a page heading above the gallery so LCP is text, not an image.
- If CLS is high: Ensure all images have explicit width/height from metadata (or aspect-ratio on container for grid mode). Check that no fonts cause layout shift (Playfair Display and Lato should already be in next/font from Phase 1).
- If unused CSS flagged: Verify lightbox CSS is NOT in the initial bundle (should only be in dynamically imported GalleryLightbox).
- If performance score below 100: Check for any client-side JS that shouldn't be there. The gallery grid should be Server Component rendered. Only GalleryClient should be client-side, and it should be minimal (just state management). Review every audit opportunity and diagnostic in the Lighthouse report.
- If score is 99 due to minor issues outside our control (e.g., third-party script overhead from SpeedInsights itself): document the reason and accept it.

**Step 6: Verify tap targets**
Check that all interactive elements in the gallery meet 44x44px minimum:
- Gallery image click targets (the button wrapping each image -- should be at least 44x44 since images are larger)
- Lightbox close button (YARL default should be sufficient, but verify)
- Lightbox navigation arrows (YARL default should be sufficient, but verify)
- If any are undersized, add min-h-11 min-w-11 (44px) to the element

**Step 7: Re-run Lighthouse after fixes**
If any changes were made, rebuild and re-audit to confirm the score reaches 100.

**Step 8: Document PERF-03 Speed Insights caveat**
In the SUMMARY, note: "PERF-03 requires Vercel Speed Insights 100/100 mobile performance. Lighthouse CLI achieves [score]/100 as development proxy. @vercel/speed-insights is installed and SpeedInsights component is rendered in the site layout. Final PERF-03 verification requires a deployed Vercel preview with real user traffic collecting Speed Insights data."

Clean up lighthouse JSON files after verification (do not commit them):
```bash
rm -f lighthouse-gallery.json lighthouse-gallery-mobile.json
```
  </action>
  <verify>
    Lighthouse mobile performance score is 100 (or 99 with documented reason). LCP under 2.5 seconds. CLS under 0.1. `@vercel/speed-insights` is in package.json. SpeedInsights component is rendered in src/app/(site)/layout.tsx. No critical performance issues flagged. `npm run build` still succeeds after any fixes.
  </verify>
  <done>
    Gallery test page achieves Lighthouse mobile Performance score of 100 (development proxy for PERF-03). LCP under 2.5s, CLS under 0.1. @vercel/speed-insights confirmed installed and rendering SpeedInsights component in layout for post-deployment real-user monitoring. Any performance issues identified and fixed.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual and interaction verification of gallery and lightbox</name>
  <files>
    src/app/(site)/gallery-test/page.tsx
  </files>
  <action>
Human verifies the complete gallery component system visually and interactively.

What was built: Complete gallery component system with masonry layout, lightbox with YARL, and all shared components (Section, PricingCard, ScarcityCue, Storyboard, AnswerBlock, JsonLd). Gallery test page at /gallery-test with 20+ images.

Verification steps:
1. Visit http://localhost:3000/gallery-test
2. Verify gallery renders 20+ images in masonry columns layout (2 cols on mobile, 3 on tablet, 4 on desktop -- resize browser to check)
3. Hover over an image -- should see subtle scale-up effect
4. Click an image -- lightbox should open with dark background, image counter "X / Y", thumbnail strip at bottom, close button (X) in top corner
5. Press right arrow key -- should navigate to next image
6. Press left arrow key -- should navigate to previous image
7. Press Escape -- lightbox should close
8. On mobile (or DevTools mobile emulation): swipe left/right on image in lightbox
9. Verify all lightbox buttons (close, nav arrows) are large enough to tap easily (44x44px minimum)
10. Check that the page loads quickly -- images should appear with blur placeholders that sharpen as they load
  </action>
  <verify>
    Human confirms all 10 verification steps pass. Type "approved" if gallery and lightbox look and work correctly, or describe any issues.
  </verify>
  <done>
    Human approves visual quality, interaction polish, and functional correctness of the gallery and lightbox system.
  </done>
</task>

</tasks>

<verification>
- Lighthouse mobile performance score of 100 documented (or 99 with reason)
- LCP under 2.5 seconds confirmed
- CLS under 0.1 confirmed
- @vercel/speed-insights installed and SpeedInsights rendered in layout
- All lightbox interactions verified: arrow keys, swipe, counter, Escape, close button
- 44x44px tap targets on all interactive elements
- Human verification of visual quality and interaction polish
- PERF-03 caveat documented: final Speed Insights verification deferred to post-deployment
</verification>

<success_criteria>
- Gallery test page achieves Lighthouse mobile Performance score of 100 (development proxy for Vercel Speed Insights target)
- LCP is under 2.5 seconds on mobile Lighthouse audit
- CLS is under 0.1 on mobile Lighthouse audit
- @vercel/speed-insights is confirmed installed and SpeedInsights component is rendered
- Lightbox opens on click, navigates with arrow keys and swipe, shows "X / Y" counter, closes with Escape
- All interactive elements have 44x44px minimum tap targets
- Human approves visual quality and interaction polish
</success_criteria>

<output>
After completion, create `.planning/phases/02-shared-components-and-galleries/02-03-SUMMARY.md`
</output>
