---
phase: 03-core-pages-and-conversion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(site)/page.tsx
  - src/components/home/Hero.tsx
  - src/components/home/PortfolioPreview.tsx
  - src/components/home/TestimonialCarousel.tsx
  - src/components/home/HomeCTA.tsx
  - src/components/testimonials/TestimonialCard.tsx
  - src/sanity/lib/queries.ts
autonomous: true
requirements: [PAGE-01, CONV-04, CONV-06, CONV-07]
must_haves:
  truths:
    - "Homepage displays a full-bleed hero section with gender-inclusive senior portrait imagery and a prominent CTA"
    - "Homepage shows a portfolio preview grid with representative images from senior and family sessions"
    - "Homepage displays client testimonials from CMS data"
    - "Homepage shows an active scarcity cue fetched from Sanity when one exists"
    - "The persistent 'Inquire for Detailed Pricing' CTA is visible in the sticky nav and within 2 scrolls on mobile"
  artifacts:
    - path: "src/app/(site)/page.tsx"
      provides: "Homepage Server Component composing hero, portfolio, testimonials, scarcity cue, CTA sections"
      min_lines: 50
    - path: "src/components/home/Hero.tsx"
      provides: "Full-bleed hero section with priority SanityImage and overlay CTA"
      min_lines: 20
    - path: "src/components/home/PortfolioPreview.tsx"
      provides: "Portfolio preview grid showcasing session types"
      min_lines: 20
    - path: "src/components/testimonials/TestimonialCard.tsx"
      provides: "Reusable testimonial display card for homepage, service pages, and city pages"
      min_lines: 15
  key_links:
    - from: "src/app/(site)/page.tsx"
      to: "sanityFetch"
      via: "GROQ queries for testimonials, scarcity cue, galleries"
      pattern: "sanityFetch.*TESTIMONIALS_QUERY|ACTIVE_SCARCITY_CUE_QUERY"
    - from: "src/components/home/Hero.tsx"
      to: "SanityImage"
      via: "priority loading for LCP"
      pattern: "SanityImage.*priority"
    - from: "src/app/(site)/page.tsx"
      to: "ScarcityCue component"
      via: "conditional render from CMS data"
      pattern: "ScarcityCue.*message.*isActive"
---

<objective>
Build the homepage as the primary landing and conversion page. Compose existing shared components (Section, ScarcityCue, GalleryGrid) with new homepage-specific components (Hero, PortfolioPreview, TestimonialCard, HomeCTA) into a scroll journey that moves visitors toward the inquiry CTA.

Purpose: The homepage is the primary entry point and must immediately communicate editorial quality, build trust via testimonials, create urgency via scarcity cues, and funnel visitors toward the inquiry form.
Output: Fully functional homepage at `/` with hero, portfolio preview, testimonials, scarcity cue, and mid-page CTA sections.
</objective>

<execution_context>
@/Users/jeff/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeff/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-pages-and-conversion/03-RESEARCH.md
@.planning/phases/02-shared-components-and-galleries/02-01-SUMMARY.md
@.planning/phases/02-shared-components-and-galleries/02-02-SUMMARY.md

Key existing components to compose:
@src/components/shared/Section.tsx
@src/components/shared/ScarcityCue.tsx
@src/components/shared/GalleryGrid.tsx
@src/components/shared/GalleryClient.tsx
@src/components/shared/SanityImage.tsx
@src/components/layout/HeaderClient.tsx (already has persistent gold CTA)
@src/lib/siteConfig.ts (NAP config and CTA config)
@src/sanity/lib/queries.ts (existing GROQ queries)
@src/sanity/lib/fetch.ts (sanityFetch wrapper)
</context>

<tasks>

<task type="auto">
  <name>Task 1: TestimonialCard reusable component and homepage-specific section components (Hero, PortfolioPreview, HomeCTA)</name>
  <files>
    src/components/testimonials/TestimonialCard.tsx
    src/components/home/Hero.tsx
    src/components/home/PortfolioPreview.tsx
    src/components/home/HomeCTA.tsx
    src/components/home/TestimonialCarousel.tsx
  </files>
  <action>
    Create 5 components:

    1. **TestimonialCard** (`src/components/testimonials/TestimonialCard.tsx`): Server Component.
       - Props: `name: string`, `quote: string`, `service?: string`, `image?: SanityImageAsset` (import type from SanityImage.tsx).
       - Renders: Blockquote with quote text in italic, attribution with name and optional service label ("Senior Session" / "Family Session"), optional circular client photo via SanityImage (64px, `sizes="64px"`).
       - Styling: `rounded-lg border border-border bg-white p-6`, consistent with brand palette.

    2. **Hero** (`src/components/home/Hero.tsx`): Server Component.
       - Props: `heading: string`, `subheading: string`, `ctaLabel: string`, `ctaHref: string`.
       - Renders: Full-bleed hero container (`relative h-[70vh] min-h-[500px] overflow-hidden`). Since we don't have a Sanity hero image yet, use a styled gradient background with brand colors (dark with gold accent) as a placeholder that Emily will replace with a real image in Sanity Studio. Include the heading in `font-heading text-4xl md:text-6xl font-light text-white`, subheading in muted white text, and a gold CTA button (`bg-brand-gold min-h-11 px-6 py-3 rounded text-white text-sm tracking-wide`) linking to `/contact`. The hero must feel gender-inclusive and editorial.
       - Note: When Sanity content is connected, this will be enhanced to use SanityImage with `priority` and `fetchPriority="high"` for LCP optimization. For now, placeholder is sufficient.

    3. **PortfolioPreview** (`src/components/home/PortfolioPreview.tsx`): Server Component.
       - Props: `categories: Array<{ title: string; href: string; description: string }>`.
       - Renders: A grid of 2 columns (`grid grid-cols-1 md:grid-cols-2 gap-6 md:gap-8`) showing senior and family session categories as cards. Each card has a placeholder image area (gradient bg matching brand), the category title in `font-heading text-2xl`, a brief description, and a "View Gallery" link styled as text with gold hover.
       - This component is intentionally simple — it will be enhanced with real Sanity gallery preview images when CMS content is populated.

    4. **HomeCTA** (`src/components/home/HomeCTA.tsx`): Server Component.
       - Props: `heading: string`, `body: string`, `ctaLabel: string`, `ctaHref: string`.
       - Renders: A centered section with heading in `font-heading text-3xl md:text-4xl`, body text in muted foreground, and a gold CTA button (`bg-brand-gold min-h-11 ...`). This mid-page CTA section breaks up the content scroll and reinforces the conversion action.

    5. **TestimonialCarousel** (`src/components/home/TestimonialCarousel.tsx`): Server Component.
       - Props: `testimonials: Array<{ name: string; quote: string; service?: string; image?: SanityImageAsset }>`.
       - Renders: A grid of TestimonialCards (`grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6`). Server-rendered grid, no client-side carousel JS. Displays up to 6 featured testimonials. Import and use TestimonialCard.
  </action>
  <verify>
    Run `npx tsc --noEmit` — no type errors.
    Run `npm run build` — clean build with no errors.
  </verify>
  <done>
    5 components exist at the specified paths. TypeScript compiles without errors. Each component follows the established project patterns (Tailwind v4 classes, font-heading for headings, brand-gold for CTAs, min-h-11 for tap targets).
  </done>
</task>

<task type="auto">
  <name>Task 2: Homepage page.tsx composition with sanityFetch data, scarcity cue, testimonials, and scroll journey</name>
  <files>
    src/app/(site)/page.tsx
    src/sanity/lib/queries.ts
  </files>
  <action>
    1. **Add GROQ queries** to `src/sanity/lib/queries.ts`:
       - `HOMEPAGE_GALLERY_PREVIEW_QUERY`: Fetch one gallery per category (senior, family) with title, slug, and preview image. Use existing GALLERIES_BY_CATEGORY_QUERY pattern.
       - `FEATURED_TESTIMONIALS_QUERY`: Alias for TESTIMONIALS_QUERY with `$featured: true, $service: null` — just document the params pattern in a comment; no new query string needed since TESTIMONIALS_QUERY already supports this.

    2. **Rewrite `src/app/(site)/page.tsx`** as the full homepage Server Component:
       - Import: Section, Hero, PortfolioPreview, TestimonialCarousel, HomeCTA, ScarcityCue, sanityFetch, GROQ queries.
       - Metadata: Keep existing metadata (title: "Emily Kathryn Photography | Senior & Family Portraits", description about South-Central Virginia). Add `openGraph` with title, description, url (`https://emilykathryn.com`), siteName, locale `en_US`, type `website`. Add twitter card `summary_large_image`. (OG image will be added in Plan 03-05.)
       - Data fetching: Use `Promise.all` to fetch in parallel:
         - Featured testimonials: `sanityFetch({ query: TESTIMONIALS_QUERY, params: { featured: true, service: null }, tags: ['testimonial'] })`
         - Active scarcity cue: `sanityFetch({ query: ACTIVE_SCARCITY_CUE_QUERY, tags: ['scarcityCue'] })`
       - Scroll journey (top to bottom):
         1. **Hero** — heading: "Your Story. Your Session. Your Moment.", subheading about editorial-style sessions in South-Central Virginia, CTA linking to /contact
         2. **ScarcityCue** — conditional render if active scarcity cue exists from CMS
         3. **PortfolioPreview** — 2 categories: Senior Portraits (/senior-portraits) and Family Portraits (/family-portraits) with brief descriptions
         4. **HomeCTA** — mid-page CTA section: "Ready to Book Your Session?" with warm copy and gold button
         5. **TestimonialCarousel** — display featured testimonials from CMS (will show empty state gracefully if no testimonials exist yet)
         6. **Final CTA** — another HomeCTA at bottom or a simpler text-based CTA with "Get in Touch" linking to /contact

       - Brand voice throughout: warm, confident, editorial. Copy speaks to both teens and parents.
       - The persistent CTA is ALREADY in the sticky nav (HeaderClient gold button). Verify it's visible. The hero CTA and mid-page CTA ensure it's accessible within 2 scrolls on mobile.
  </action>
  <verify>
    Run `npm run build` — homepage builds without errors.
    Run `npm run dev` and visit `http://localhost:3000` — homepage renders with all sections in correct order.
    Verify the gold CTA is visible in the sticky nav AND within the hero section (within 2 scrolls on mobile).
  </verify>
  <done>
    Homepage at `/` displays hero, conditional scarcity cue, portfolio preview, mid-page CTA, testimonials, and bottom CTA. Data is fetched from Sanity via sanityFetch. The persistent CTA is in the nav bar. Metadata includes OpenGraph fields.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` completes successfully
2. Homepage renders all sections in the correct scroll order
3. ScarcityCue renders conditionally based on CMS data (null when no active cue)
4. TestimonialCard component is reusable (will be imported by service pages and city pages in other plans)
5. Gold CTA button meets 44px minimum tap target (min-h-11)
6. Homepage metadata includes OpenGraph fields
</verification>

<success_criteria>
- Homepage scroll journey renders: Hero > ScarcityCue > PortfolioPreview > HomeCTA > Testimonials > Bottom CTA
- sanityFetch fetches testimonials and scarcity cue in parallel
- TestimonialCard is a standalone reusable component at src/components/testimonials/TestimonialCard.tsx
- The "Inquire for Detailed Pricing" CTA is visible in sticky nav AND within 2 scrolls on mobile (hero CTA covers this)
- Build passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-pages-and-conversion/03-01-SUMMARY.md`
</output>
